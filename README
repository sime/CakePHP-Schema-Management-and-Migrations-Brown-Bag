"Schema management and migrations in CakePHP" Brown Bag @ Pollenizer

* `generate` schema file
	* -f param
* `create` and dump tables based on schema file
* `update` alter tables based on schema file
